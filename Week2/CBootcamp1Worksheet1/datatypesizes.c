#include <stdio.h>
#include <limits.h>
#include <float.h>

int main() {
    printf("The maximum size of an Integer is: %d\n", INT_MAX);
    printf("The maximum size of a float is: %d\n", FLT_MAX);
    printf("The maximum size of a Double is: %d\n", DBL_MAX);
    printf("The maximum size of a Char is %d\n", CHAR_MAX);

    return 0;
}